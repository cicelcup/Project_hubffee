<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hubffee">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Screen1"
            android:label="@string/pantalla_1">
            <!--Main activity is the parent activity of Screen 1-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.hubffee.MainActivity" />
        </activity>
        <activity
            android:name=".Screen2"
            android:label="@string/pantalla_2">
            <!--Main activity is the parent activity of Screen 2-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.hubffee.MainActivity" />
        </activity>
        <activity
            android:name=".Screen3"
            android:label="@string/pantalla_3">
            <!--Main activity is the parent activity of Screen 3-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.hubffee.MainActivity" />
        </activity>
        <activity
            android:name=".Screen4"
            android:label="@string/pantalla_4">
            <!--Main activity is the parent activity of Screen 4-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.hubffee.MainActivity" />
        </activity>
        <activity
            android:name=".Screen5"
            android:label="@string/pantalla_5">
            <!--Main activity is the parent activity of Screen 5-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.hubffee.MainActivity" />
        </activity>
        <activity
            android:name=".Screen6"
            android:label="@string/pantalla_6">
            <!--Main activity is the parent activity of Screen 6-->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.example.hubffee.MainActivity" />
        </activity>
        <!--Main activity that loads each time that the app starts-->
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>